in the future should implement solverfactory to fpuzzles url for the new constraints

figure out why it hangs
possibilities:
 - memory leak
 - infinite loop
print out current midnight cell id with each cur variation has no sols to figure out when hang